# metronome（SetClick）作業ログ

最新が上。

## 2026-09-20（6）
### v1.8.0 — Koki の決定で保存モデルを簡素化（共通ミキサー）
- Koki 決定：未保存ポップアップ不要／曲の BPM は編集画面でだけ変える／再生中の ± は保存しない／つまみの量は自動保存／ON/OFF は 1拍目と表だけが基本で、操作したときだけ ON・保存しない
- 実装：下書き（drafts）と「未保存の変更」バーを撤去。± は再生中だけ（停止中は無効）。ミキサーを全曲共通の1つに（音量 `S.settings.levels` 自動保存、ON/OFF `layerOn` はセッションのみ）。つまみを回す・キーを押すと ON、0 まで絞ると OFF。OFF のレイヤーも保存済み音量を暗いリングで表示
- 曲ごとの裏設定を廃止：曲リストのチップは拍子だけ、編集画面からミキサーを外す、まとめて追加の「8分/3連/16分」は読み飛ばすだけ、共有リンクの4番目は 'none'（旧版互換）
- 検証（440×956）：起動時 1拍目・表のみ ON で accent/beat だけ発音、キーで 8分裏 ON→発音・音量は不変、つまみで 3連 ON＋音量保存、± は再生中だけで曲・保存データ不変・停止で 180 に戻る、曲移動で ON/OFF 維持、再読み込みで ON/OFF 初期化・音量は保持、フリーと共通、コンソールエラーなし
- 戻し先タグ `v1.7.0` を追加
- 関連ファイル: index.html, CLAUDE.md

## 2026-09-20（5）
### v1.7.0 — レビュー2回目：保存モデルの統一・フリーの主操作固定・リスト化
- レビュアーの質問「つまみの即保存は選んだのか、副作用か」→ 正直に副作用だったと回答。保存モデルを意図して決め直した
- **保存モデル**：± とミキサーを曲ごとの下書きに統一、明示保存。止めても・曲を移っても消えない（「182→停止→180 に黙って戻る」を解消）。プレイヤー上に「未保存の変更［戻す］［保存］」、行に「未保存」チップ、BPM は変更色
- ± を停止中も操作可に（中途半端の解消）
- **バグ修正**：停止中のドックのミキサーが「最後に鳴った曲」の値を基準にしていた（v1.6）→ 対象曲の値に
- フリー：再生ボタンを下端に固定（v1.6 で 956px 画面の外に出ていた後退）、「スタート」→「再生」に統一
- ±5/±1 を「浮く」段へ（テンポを動かす部品の段を統一）
- 曲の行を枠なし・区切り線のリストに（枠つき矩形はボタン専用）。再生中の強調は左バー＋BPM 色の2つに
- 置いて見る距離対策：拍 LED 12→18px、BPM 7セグ 36→44px、次の曲を 13px の専用行に
- 編集ツールの空きマス解消（3＋2）
- 検証（440×956）：フリーの再生ボタン 890–946px で画面内、最下部までスクロールで「追加」がバーに隠れない、下書き7シナリオ（停止中±・下書きで再生・停止後も保持・曲移動と復帰・戻す・保存・編集画面との連携）、全画面で 44px 未満 0・文字5段階、コンソールエラーなし
- 未確認（実機のみ）：ノブの縦ドラッグとスクロールの干渉、セーフエリア込みの収まり
- 関連ファイル: index.html, CLAUDE.md

## 2026-09-20（4）
### v1.6.0 — 外部レビューを仕分けてデザイン整理
- Koki が受けたデザインレビュー（「全要素に同じベベル」「LED 5個」「装飾」「寸法に体系なし」「タップ領域 17px」等）を仕分け。前提の「ステージ機材として床置きで読める」は Koki の意図と違う（手元で使う）ので不採用、それ以外の実質的指摘はほぼ採用
- 戻し先タグ `v1.5.0` を追加（`v1.4.0` も残す）
- 3階層化（面は平ら／触る部品だけ浮く／表示窓だけ沈む）、二次ボタン・タブ・選択肢は罫線＋アンバー下線に
- LED はドックの1つだけ（リスト BPM は等幅数字）。英字小見出し・サブタイトル・背景の光・待機中の左バー・ネジ・消灯ドットを削除
- 文字 11/13/16/18/24px の5段階、角丸 6/10/16px、タップ領域すべて 44px 以上（計測で確認）
- ミキサー：ノブ＋独立した 44px の ON/OFF キー（LED 付き）。停止中も操作でき、変更は即その曲に保存。± の BPM だけ一時変更で「○ BPM を保存」ボタン（ドック上に浮かぶ 44px）
- セトリ削除を編集ツールから離して下部に単独配置。画面フラッシュは「オフ／オン」の選択式に（「ステージ用」表記は削除）。操作説明文（ミキサー・TAP・空状態）を削除。再生ボタンは単色
- 検証（440×956）：全画面で 44px 未満の操作部品 0、文字サイズ 5 種、影 27→16・グラデ 25→13、停止中ミキサーの保存、再生中 ON/OFF の即保存、BPM 保存ボタン
- 関連ファイル: index.html, CLAUDE.md

## 2026-09-20（3）
### v1.5.0 — DTM／スタジオ機材風デザインに全面変更
- Koki 要望：DTM ソフトのようなダイアル・音楽ソフト寄りの UI に。ただし「今の状態にすぐ戻せるように」
- 戻し先として **git タグ `v1.4.0`** を作成・push（手順は CLAUDE.md）。データ形式は変えていないので戻しても安全
- ノブ：SVG で金属キャップ・ローレット・指示線・270° の発光リング（1拍目 赤／表 白／8分裏 琥珀／3連 シアン／16分 紫）。上下ドラッグ・ホイール・矢印キー、名前タップで ON/OFF（LED 付き）
- BPM：7セグ LED（SVG 自作、消灯セグメントも薄く表示）。ドック・曲リスト・フリー/編集の大型 LCD（タップで数値直接入力）
- 拍表示を LED ランプ（緑、1拍目は赤）に。キーは押し込み表現、選択中は琥珀色に点灯。ドックはビス付き金属パネル、トーストも LCD 風、設定トグルは LED スイッチ、BPM スライダーはフェーダーキャップ風
- 写真素材は使わず SVG/CSS で描画（ライセンス・解像度・オフライン容量の観点）
- 検証：ノブのドラッグ 35px→25%・回転角/リング/保存表示の連動、キーボード操作、LCD 直接入力、編集キャンセルで曲不変、PC 幅で 640px 中央・横はみ出しなし、コンソールエラーなし
- 関連ファイル: index.html, CLAUDE.md

## 2026-09-20（2）
### v1.4.0 — Dr.Beat 式ミキサー（各音を個別に音量調整）
- Koki 要望：表・裏・3連…をそれぞれ音量調整したい（UI 形式は問わない、調整できることが目的）
- 「裏を1つ選ぶ＋設定の全体バランス」の2つの仕組みを、曲ごとの**ミキサー1つ**（1拍目／表／8分裏／3連／16分、0で無音・複数同時可）に統合。ダイアルではなく縦フェーダー（タッチで回すつまみは不正確、バーの高さで量が読める）。名前タップでオン/オフ（切る前の値を記憶）
- エンジンを 1拍=12ティック（6/8系6・x/8系2）のグリッドに変更。重なる位置は最大音量のレイヤーで1回だけ鳴らす。レイヤー別 GainNode で再生中の操作が即反映
- ドック・編集画面・フリーの3か所に同じミキサー。ドックは ± と同様に一時変更→「保存」で BPM とミックスを曲へ
- 旧データ（sub＋全体バランス＋1拍目トグル）は読み込み時に各曲のミックスへ変換。共有リンクはミックス配列を載せ、旧形式リンクも読める。まとめて追加は「16分 8分」など複数指定で重ねがけ
- 設定から「バランス」「1拍目を高い音に」を削除（ミキサーの1拍目フェーダーが代替）
- 検証：180BPM で 8分裏70＋3連55 が 0/0.111/0.167/0.222/0.333s に正確に並ぶ、16分と8分の重なりで二重発音なし、3連のみで出力 0.446・全0で 0、名前タップの記憶復帰、保存→一覧に「8分裏」「3連」、6/8・7/8 のフェーダー構成、1小節停止、試聴中のミキサー反映
- 関連ファイル: index.html, CLAUDE.md, README.md

## 2026-09-20
### v1.3.0 — プレイヤーに「裏」のワンタップ切替
- Koki 要望：セトリから流しているときもボタン一つで裏を鳴らしたい（表の音量を 0 にする必要はない）
- ドックに `裏 なし｜8分裏｜3連｜16分`（6/8 系は なし｜8分｜16分）。再生中だけ押せる。表はそのまま鳴り、裏が足される
- `Engine.setSub`：次の拍頭から新しい分割で刻み直し、小節内の位置を保つ（1/2小節停止の終点も換算）。フリー・試聴の裏変更も再スタートせずこれを使うように
- ± と同じく一時変更扱いで、BPM か裏が曲の設定と違うと「保存」が出る（両方を曲に書き戻す）。次の曲へ進むとその曲の設定に戻る
- 設定の「表拍を0%にすると裏だけ」注記は誤解を招くので削除
- 検証：113BPM で表4拍→8分裏へ切替後も次の1拍目が 4×0.531s 後ぴったり、16分 0.1327s、6/8 の16分 0.1724s、保存→一覧に「8分裏」、次の曲は自分の設定、1小節停止＋途中切替で小節末に停止
- 関連ファイル: index.html

## 2026-09-19
### v1.2.0 — 複数セトリの一括共有リンク＋SW をネット優先に
- Koki の依頼：YouTube Music のキュー（スクショ）からアーティスト別セトリを作成（ゲス除く。andymori・NEE・ハヌマーンは1つにまとめる）→ 9セトリ・32曲
- BPM は推測せず裏取り：songbpm.com（Spotify 解析由来の自動値）× ongakumichi523.jp（手計測ブログ）× chord-rinne.jp（「原曲BPM」表記）。尺がスクショと一致する音源で照合
  - 2出典以上一致 13曲、倍/半分のズレ 3曲（手計測側を採用：5150・Red Criminal 192、シャンデリア・ワルツ 242。TRAIN は両者に共通する 133）、自動解析のみ 16曲はメモ「要確認（自動解析のみ）」
  - 検索エンジンの要約が「最終列車 BPM 68」と出したが、実ページを開くと**同名の別アーティスト曲**だった → 採用せず。要約の数値は必ず一次ページで確認する
  - Tunebat は Cloudflare のボット検証が出るため使わない（突破しない）。bpm-database.tokyo は 500 で停止中
- アプリ側：共有リンクが1セトリ単位だったため、複数セトリ用 `#importAll=` を追加（旧版は無視する別名）。設定に「全セトリをリンクで共有」。SW v2 はページ本体ネット優先（v1 はキャッシュ優先で常に1版遅れ）
- 旧 v1.1 がキャッシュされた端末からの更新経路を再現検証：初回は旧版が開きリンクは無害に無視、2回目で新版＋「9件のセトリを取り込みますか？」。オフライン起動も確認
- セトリタブの中央寄せが PC 幅で計算ずれ → getBoundingClientRect 基準に修正
- 関連ファイル: index.html, sw.js

## 2026-09-18
### 公開
- Koki の承認を得て `isojz/setclick`（public）を作成・push、GitHub Pages を有効化 → https://isojz.github.io/setclick/

### v1.1.0 — 初期実装（単一 HTML の PWA）
- Koki の要望「複数セトリを保存、曲ボタンをタップでその BPM で鳴る」→ SetClick を新規作成
- 途中で追加要望：拍子（4拍子・3拍子…）と裏の選択、裏だけ別の音量、セトリ1＝曲1,2,3… という複数セトリの見せ方、自分で曲・セトリを登録できること
- 実装した主な機能：セトリ切替タブ／曲タップで即再生・即切替／拍子 11 種＋裏（8分裏・3連・16分）／全体＋1拍目・表拍・裏拍の音量／再生中 ± 微調整→保存／TAP テンポ／フリーモード／1・2小節で自動停止／画面フラッシュ／Wake Lock／まとめて追加（「曲名 BPM [拍子] [裏]」を1行ずつ）／共有リンク（貼り付け取り込みにも対応）／JSON バックアップ／オフライン対応
- 自己レビューで直した点：iPhone のホーム画面アプリは Safari と保存領域が別→共有リンクを貼り付けて取り込めるように／最後のセトリも削除可に（空の「セトリ1」を残す）／6/8 系は裏の既定を8分に（付点4分だけだと2クリック/小節で合わせにくい）／キーボード操作の説明は PC だけに表示／元に戻すトーストを 6 秒に
- 検証（内蔵ブラウザ、`?debug` の発音ログ・出力ピーク値で数値確認）：120BPM 8分裏で間隔 0.25s 厳密、58BPM 6/8 で 0.3448s、再生中 +3 で位相を保ったまま 0.3333→0.3279s、1小節停止で「a b b b」後に自動停止、ミキサー（全体 0.80／裏のみ 0.45／表のみ 0.67／ミュート 0）、旧データ移行、共有リンク往復、SW キャッシュ6ファイル
- 関連ファイル: index.html, sw.js, manifest.webmanifest, tools/serve.js, tools/make_icons.py
