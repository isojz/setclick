# hobby/metronome — SetClick（セトリ・メトロノーム）

## 目的
Koki（ドラム）がリハ・ライブで使うメトロノーム。セトリを複数保存し、曲をタップするとその曲の BPM・拍子で即クリックが鳴る。

## 構成
- **単一 HTML の PWA**（ビルドなし）：`index.html` / `manifest.webmanifest` / `sw.js` / `icon-*.png`
- データは端末の localStorage（キー `setclick:v1`）。サーバー・クラウド同期なし
- 公開 URL：**https://isojz.github.io/setclick/** （GitHub Pages、main ブランチ直下。push で自動反映）
- リポジトリ：https://github.com/isojz/setclick （public。コードのみで個人データは含まない）

## 設計上の要点（変えるときは理由を理解してから）
- **発音は Web Audio の先読みスケジューラ**（Worker の 25ms tick → 120ms 先まで AudioContext 時刻で予約）。setTimeout で直接鳴らすとテンポが揺れる
- **再生系のボタン（再生／停止・前後の曲・フリーの再生）は押した瞬間に動く**（`pressAction`、pointerdown）。音声がまだ起動していない最初の1回だけは、自動再生制限のため指を離した時の click で動かす（`Engine.ready`）。1拍目は押してから 20ms 後（`START_LEAD`、v1.8.0 までは 60ms）。曲の行はスクロールと区別するため click のまま
- **再生中のテンポ変更**は「次に鳴る予定の拍」を起点に組み直して位相を保つ（`Engine.setBpm`）
- **拍子モデル**：曲は `ts`（'4/4' 等）を持つ。6/8・9/8・12/8 は付点4分を1拍、5/8・7/8 は8分を1拍として数える（`meterInfo` / `toEngine`）
- **ミキサー（v1.4〜、Dr.Beat 式）**：曲ごとに `mix = {accent, beat, e8, t3, s16}`（0〜1）。0 のレイヤーは鳴らない・複数同時に鳴る。エンジンは1拍を `GRID`（4分系12・6/8系6・x/8系2）ティックに刻み、各ティックに乗るレイヤーのうち最大音量のものを1回だけ鳴らす（`TICKS`）。レイヤーごとに GainNode を分けてあるので再生中のフェーダー操作が即効く。旧 `sub` 形式は読み込み時に `mixFromSub` で変換
- **音量**：設定は全体（master）だけ。各レイヤーはミキサー（ドック・編集画面・フリー）
- iOS 対策：`navigator.audioSession.type='playback'`（マナーモード対策）、古い iOS は無音 audio ループで代替。再生中は Wake Lock
- iPhone はホーム画面アプリとブラウザで保存領域が別 → 共有リンクは「新しいセトリ」画面に貼り付けても取り込める
- **共有リンクの曲配列**：`[曲名, BPM, 拍子, ミックス, メモ]`。ミックスは v1.4 以降 `[1拍目,表,8分,3連,16分]`（0〜100 の配列）、それ以前は裏のキー文字列。読み込みは両対応
- **共有リンクの形式**：1件は `#import=`（v1.1 互換、payload `{v:2,n,s}`）、複数件は `#importAll=`（`{v:3,L:[{n,s}]}`）。`#importAll=` は v1.1 が無視する（誤読しない）ように意図的に別名にしている。**形式を変えるときは、キャッシュに残った旧版が新形式をどう扱うかを必ず考える**
- Service Worker（v2〜）：ページ本体はネット優先・2.5 秒でキャッシュにフォールバック、アイコン等はキャッシュ優先。v1（キャッシュ優先）時代の端末は、更新後の初回だけ旧版が開き、2回目から新版になる。**アセット構成を変えたら `sw.js` の `CACHE` 番号を上げる**。localhost では `?sw` を付けたときだけ登録
- 旧版→新版の更新経路は、別ポートで旧ファイルを配信 → SW 登録 → 新ファイルに差し替え → 別オリジンから遷移、で再現できる（2026-09-19 に実施）

## デザイン（v1.6〜 スタジオ機材風・整理版）と戻し方
- **想定シーン**：練習・リハ。演奏中は両手がふさがるので、スマホは譜面台やアンプに置いて腕の距離（50〜100cm）から見る。ステージ床置きは主目的ではない（Koki 明言 2026-09-20）
  - 置いた距離で読むもの＝曲名・BPM・次の曲・拍ランプ → ここは大きく（ドックの LED 18px、BPM 7セグ 44px、次の曲 13px の専用行）
  - 44px は「押せるか」の基準。「置いて読めるか」は別に詰める（将来案：再生中の大表示モード）
- **ルール（崩さないこと）**
  - 階層は3つだけ：パネル＝平ら／演奏中に触る部品（再生系・ノブ・±・±5/±1・TAP・ミキサーの ON/OFF）＝浮く／表示窓（液晶・入力）＝沈む。二次ボタン・タブ・選択肢は平らな罫線＋選択時アンバー下線
  - 枠つきの矩形はボタン専用。曲の行は内容なので枠なし・区切り線だけのリスト
  - 主操作は画面下端に固定（セトリ＝プレイヤー、フリー＝再生バー）。スクロールしないと押せない位置に置かない
  - LED（7セグ `seg7()`）は1画面に1つ（ドック、フリー/編集の大型液晶）。リストの BPM は普通の等幅数字
  - 文字は `--fs-xs/sm/md/lg/xl` = 11/13/16/18/24px の5段階のみ。角丸は 6/10/16px のみ。タップ領域は最小 44px（`--tap`）
  - 英大文字の飾り見出し・ネジ・背景の光など意味のない装飾を足さない。ジェスチャーを文章で説明しない（見た目で伝わる部品にする）
- **保存モデル（v1.8〜、Koki が決定 2026-09-20）**
  - 曲の BPM・拍子は**編集画面でだけ**変える。プレイヤーの ± は再生中だけのその場限り（保存しない・停止中は押せない）
  - ミキサーは**全曲共通で1つ**。つまみの量（音量）は触ると自動保存（`S.settings.levels`）。ON/OFF は保存しない（`layerOn`）：起動時はいつも 1拍目と表だけ ON、つまみを回すか ON/OFF キーを押したレイヤーだけ ON。曲を移っても ON/OFF はそのまま、再読み込みで初期状態へ
  - 曲ごとの裏設定（旧 `song.mix`）は使わない（データには残っているが読まない）。6/8 も最初は1拍目と表だけ
  - 未保存ポップアップ・下書き（v1.7）は不要と判断され撤去
- 教訓：操作系の挙動を変えるときは保存・取り消しの扱いを先に決める（v1.6 は副作用で即保存になっていた）
- ノブ・7セグは SVG で描画（写真はライセンス・解像度・容量の問題）
- **戻し先タグ**（データ形式は共通なので戻してもデータは壊れない）
  - `v1.7.0`：下書き＋明示保存モデル（曲ごとの裏設定あり）
  - `v1.6.0`：レビュー1回目反映（ミキサー即保存・フリーの再生ボタンが画面外になる問題あり）
  - `v1.5.0`：機材風の初版（レビュー指摘前。装飾・LED 多め）
  - `v1.4.0`：機材風にする前の平らなデザイン
  ```
  git checkout v1.5.0 -- index.html   # または v1.4.0
  # APP_VERSION を上げてから commit → push（GitHub Pages に反映）
  ```

## 開発・検証
- ローカル: `node tools/serve.js` → http://localhost:8741 （`hobby/.claude/launch.json` に `setclick` として登録済み）
- `?debug` を付けると `window.SetClick`（Engine / state / parseBulk）が生え、`Engine.log` に発音予定時刻、`Engine.peak()` で実出力のピーク値が取れる。耳で聞けない環境でもテンポ・音量バランスを数値検証できる
- アイコン再生成: `py -3 tools/make_icons.py`（素の `python` ではなく `py -3`。Pillow 入り）

## 未決・今後の候補
- クラウド同期（端末間共有）：必要になったら検討。今は共有リンク＋JSON バックアップで代替
- 曲ごとのカウント小節数・変拍子のアクセント位置（7/8 の 2+2+3 など）
- BPM 自動取得（2026-09-20 に検討のみ）：YouTube から音声を取る案は規約違反で、サーバーも要るため不採用。有力なのはマイクで曲を聴かせて端末内で解析する案（倍/半分の補正ボタン付き、TAP の隣に置く）。DB は Spotify audio-features が 2024-11 から新規アプリに非公開、AcousticBrainz は 2022 年に収集を終了、Deezer API は bpm 欄が 0 の曲が多い
